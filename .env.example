# Required: Google API Key for Gemini LLM and embeddings
GOOGLE_API_KEY=your_google_api_key_here

# Tavily API Key for web search (required only if AGENT_MODE=online)
TAVILY_API_KEY=your_tavily_api_key_here

# Agent mode (offline or online)
# - offline: Uses only local documentation
# - online: Can perform web searches for latest information
AGENT_MODE=online

# LLM model to use
LLM_MODEL=gemini-2.5-flash

# Embedding model for vector store
EMBEDDING_MODEL=models/text-embedding-004

# Optional: Data directory path (inside container)
DATA_DIR=/app/data

# Optional: ChromaDB configuration (set by docker-compose)
CHROMA_HOST=chroma
CHROMA_PORT=8000
